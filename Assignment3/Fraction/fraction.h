#ifndef FRACTION_H
#define FRACTION_H

#include <iostream>
#include <string>
#include <cstdlib>

#include "Fraction_global.h"

using namespace std;

class FRACTION_EXPORT Fraction
{
public:
    Fraction();
    Fraction (int);
    Fraction(int, int);
};


#endif // FRACTION_H
